# Server Module

The FastAPI HTTP API, a text-command CLI surface, and the DAIDE TCP server. All three route
through `GameService`, the single entry point into the game engine.

The live, authoritative endpoint list is the generated OpenAPI schema at
**http://localhost:8000/docs**. This file is the orientation map.

## Configuration

| Variable | Purpose |
|---|---|
| `SQLALCHEMY_DATABASE_URL` | PostgreSQL connection URL. |
| `DIPLOMACY_JWT_SECRET` | JWT signing secret. |
| `DIPLOMACY_CORS_ORIGINS` | Allowed CORS origins (default `*`). |
| `DIPLOMACY_LOG_LEVEL` / `DIPLOMACY_LOG_FILE` | Log level (default `INFO`); file instead of stdout. |

Logs cover startup and shutdown, every processed command, errors, and game state changes
(creation, player add/remove, phase advance), formatted as `[timestamp] LEVEL logger: message`.

## Auth and authorization

Two authentication modes coexist and resolve to the same `user_id`:

- **JWT Bearer** — `Authorization: Bearer <access_token>`, used by the browser client.
- **`telegram_id`** — in the request body (or as a query param on GET routes, which have no
  body), used by the Telegram bot.

The dependency `get_current_user_or_telegram` accepts either. Every endpoint that acts for a
power checks that the caller is the assigned user and returns **403** otherwise. Users must
be registered before any player action.

## Endpoints by area

### Auth (`/auth`)

`register`, `login`, `token`, `refresh`, `me`, `forgot_password`, `reset_password`,
`me/link_code`, `telegram/link`, `me/unlink_telegram`.

`forgot_password` always returns 200 (no email enumeration) and sends a reset link by mail
when `DIPLOMACY_SMTP_HOST` is configured. `telegram/link` takes no JWT — the bot calls it
with a `telegram_id` and a short-lived code generated by `me/link_code`.

### Users

`POST /users/persistent_register`, `POST /users/register` (in-memory session),
`GET /users/{telegram_id}`, `GET /users/{telegram_id}/games`, `GET /users/me/games`.

### Games

Create and list games; `join`, `quit`, `replace`, `start`; `GET /games/{id}/state` (the
GameState-native view — see [`data_spec.md`](../../docs/specs/data_spec.md) §4);
`players`; `deadline` (GET and POST); `process_turn`; `history/{turn}`; `snapshot`,
`snapshots`, and `restore/{snapshot_id}`; `draw_vote`, `draw_vote_status`, `concede`.

`POST /process_turn` accepts `require_all=true` to refuse processing until every power has
submitted; the deadline scheduler never passes it.

### Orders

`POST /games/set_orders`; `GET /games/{id}/orders` and `/orders/{power}`;
`POST /games/{id}/orders/{power}/clear`; `GET /games/{id}/orders/history`;
`GET /games/{id}/orders_status`; and **`GET /games/{id}/legal_orders/{power}`** (plus a
per-unit variant), the phase-aware enumeration of everything legal right now — what the
frontend and the bot's interactive order UI are built on.

### Messages, maps, channels

Private messages, broadcasts, and message history under `/games/{id}`. Board, orders, and
resolution PNGs via `/games/{id}/map` and `/games/{id}/generate_map[/orders|/resolution]`,
plus `/games/{id}/map/history/{turn}` and `/maps/{map_name}/preview.png`. Channel linking,
settings, posting, and analytics under `/games/{id}/channel/…`.

### Admin, dashboard, health

`/admin/*` (delete all games, cache management, counts) and `/dashboard/api/*` (systemd
status and restart, logs, read-only DB inspection) both require the `X-Admin-Token` header.
`GET /health` and `GET /health/environment` are open.

## Errors

Responses carry `status: "ok"` or `status: "error"`; errors include a descriptive `message`
and often an `error_code` (`GAME_NOT_FOUND`, `POWER_NOT_FOUND`, `INVALID_ORDER`, …).
Standard HTTP codes apply — notably **403** for acting on a power you don't control and
**409** for a stale write (someone else advanced the phase first).

## CLI surface

`Server` (`server.py`) processes text commands and is used by tests; the HTTP API does not
depend on it.

```python
from server.server import Server
server = Server()
game_id = server.process_command("CREATE_GAME standard")["game_id"]
server.process_command(f"ADD_PLAYER {game_id} FRANCE")
server.process_command(f"SET_ORDERS {game_id} FRANCE A PAR - BUR")
server.process_command(f"PROCESS_TURN {game_id}")
print(server.process_command(f"GET_GAME_STATE {game_id}"))
```

## DAIDE

`daide/` implements the real DAIDE wire protocol (binary DCSP framing, token streams) and
listens on **port 8432**, started alongside the API. Real DAIDE bots connect, handshake
(IM/RM), negotiate (NME/HLO/MAP/MDF), and submit orders (SUB/THX) against a live
`GameService` game. Press content is relayed opaquely rather than parsed — a deliberate,
permanent scope limit. See [`architecture.md`](../../docs/specs/architecture.md).
